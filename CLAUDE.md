@AGENTS.md

## Agent skills

### Issue tracker

GitHub Issues (`Scaletopia-X-Moiz/Scaletopia-Inventory`), via the `gh` CLI — a separate tracker from this repo's in-app `/tickets` feature. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`), used as-is. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

