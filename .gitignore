.claude/

# next.js
/node_modules
/.pnp
.pnp.js
/.next/
/out/
/build
.DS_Store
*.pem
.swc/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*
env.local
!.env.example

# typescript
*.tsbuildinfo

# local test data (large CSV/XLSX exports, kept out of git)
/testcsv/

# supabase local CLI link state (local machine only) — but keep the edge
# function SOURCE tracked; it's application code, not machine state.
/supabase/*
!/supabase/functions/
.vercel

# scratch scripts / one-off debug & verify runs (local only)
/.scratch/

# large local spreadsheet exports (kept out of git)
/*.numbers
